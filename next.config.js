/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true, // iyi bir geliştirme pratiği
  images: {
    domains: [
      'ui-avatars.com',
    ],
  },
};

module.exports = nextConfig;
